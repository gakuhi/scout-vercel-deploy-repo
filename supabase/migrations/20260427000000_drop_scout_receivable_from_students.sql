-- =============================================================
-- students.scout_receivable カラムを削除する。
--
-- 背景:
--   PR #180 で追加した「スカウト受信」トグルと、プロフィール編集の
--   「プロフィールを企業に公開」トグルが意味的に重複していたため、
--   1 本 (is_profile_public) に統合した。
--   スカウト受信可否は is_profile_public の値で表現する。
-- =============================================================

ALTER TABLE students
  DROP COLUMN IF EXISTS scout_receivable;
